@model IEnumerable<BancoBancado.Models.CaixaEletronicoModel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

CaixaEletronico
<html>
<head>
    <title></title>
    <link rel="stylesheet" type="text/css" href="esboco.css">
</head>

<body>
    <div class="caixa">
        <div>
            <h1>Saque Digital</h1>
        </div>
        <div class="valor">
            <b>Valor </b><input id="" /valor></input>
                         <p>
                             Notas disponíveis:
                             @foreach (var item in Model)
                             {
                                 <text>R$</text> @Html.DisplayFor(modelItem => item.Cedula)<text>,00; </text>

                             }
                         </p>
        </div>
        <button class="botao"><strong>Sacar</strong></button>
    </div>

</body>
</html>

<p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Cedula)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Quantidade)
            </th>
            <th></th>
        </tr>

    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Cedula)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Quantidade)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.id }) |
                @Html.ActionLink("Details", "Details", new { id=item.id }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.id })
            </td>
        </tr>
    }

</table>
